module.exports = () => {
  // emit new commands to server
  // add command to eventQueue with timestamps (used to recalculate orb positions when new state is received)
  // update client orb headings and selection based on new commands
}